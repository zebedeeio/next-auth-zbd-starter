import Layout from "@/components/layout";
import Contact from "@/components/Contact";

const contact = () => {
  return (
      <Layout>
        <Contact></Contact>
      </Layout>
  );
};

export default contact;
