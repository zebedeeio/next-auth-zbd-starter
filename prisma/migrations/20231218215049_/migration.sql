-- CreateTable
CREATE TABLE "Sponsors" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "company" TEXT NOT NULL,

    CONSTRAINT "Sponsors_pkey" PRIMARY KEY ("id")
);
